# 2026-06-07

## 风扇 Wind Emitter 物理改造

- 将装饰性 StudioFan 转为真实物理 Wind Emitter，逐粒子施加风力
- cloth.ts 新增 FanEmitter/FanEmitterConfig 类型、buildFanEmitter、applyWindEmitter 方法
- 风力计算：锥形范围检测 + 距离衰减 + 角度衰减 + turbulence noise + pulse
- 新建 FanComponent.tsx：可拖拽位置、叶片旋转速度随 strength 变化、方向箭头、debug cone/流线
- App.tsx 新增风扇控制面板（右侧）：Enable/Strength/Radius/ConeAngle/Turbulence/Pulse/PosXYZ/RotYX/Debug toggle
- 关键修复：cloth group 有 rotation+position 偏移，需在 useFrame 中将 fan 世界坐标转换到 cloth 局部空间
- Git 初始 commit c77a62e4 保存了鼠标交互完成时的安全基线
- TypeScript 和 Vite build 均通过，dist 已更新

## 风扇修复（第 2 轮）

- 修复风扇模型错位：电机、护罩、叶片全部对齐到局部坐标 [0, 0.94, 0]，电机沿 Z 轴、护罩在 XY 平面、叶片绕 Z 旋转
- 修复方向箭头/调试 cone 的双重旋转问题
- **修复核心 bug**：applyWindEmitter 在 update 之前调用，但 update 第一行 `particle.force.set(0, -gravity, 0)` 直接抹掉了风力。改为在 update() 内部粒子循环中施加风力（force reset 之后、积分之前）
- 默认风力强度从 8 提升到 12，物理层加 2.5x 倍率让视觉效果更明显
- 提交 772d91e
